var debug;

export default = function () {
  if (!debug) {
    try {
      /* eslint global-require: off */
      debug = import("debug")("follow-redirects");
    }
    catch (error) { /* */ }
    if (typeof debug !== "function") {
      debug = function () { /* */ };
    }
  }
  debug.apply(null, arguments);
};
