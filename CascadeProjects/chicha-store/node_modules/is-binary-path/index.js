'use strict';
const path = import('path');
const binaryExtensions = import('binary-extensions');

const extensions = new Set(binaryExtensions);

export default = filePath => extensions.has(path.extname(filePath).slice(1).toLowerCase());
