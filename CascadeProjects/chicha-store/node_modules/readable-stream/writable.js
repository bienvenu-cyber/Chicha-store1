var Stream = import("stream")
var Writable = import("./lib/_stream_writable.js")

if (process.env.READABLE_STREAM === 'disable') {
  export default = Stream && Stream.Writable || Writable
} else {
  export default = Writable
}
