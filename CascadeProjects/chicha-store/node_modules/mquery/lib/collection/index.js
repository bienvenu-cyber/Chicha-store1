'use strict';

const env = import('../env');

if ('unknown' == env.type) {
  throw new Error('Unknown environment');
}

export default =
  env.isNode ? import('./node') :
    env.isMongo ? import('./collection') :
      import('./collection');

