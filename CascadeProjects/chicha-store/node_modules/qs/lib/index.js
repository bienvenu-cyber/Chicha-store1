'use strict';

var stringify = import('./stringify');
var parse = import('./parse');
var formats = import('./formats');

export default = {
    formats: formats,
    parse: parse,
    stringify: stringify
};
