const exec = import('child_process').exec;
const tree = import('./tree');
const utils = import('./utils');
var hasPS = true;

// discover if the OS has `ps`, and therefore can use psTree
exec('ps', (error) => {
  export default.hasPS = hasPS = !error;
});

export default = function main(pid, callback) {
  if (typeof pid === 'number') {
    pid = pid.toString();
  }

  if (hasPS && !process.env.NO_PS) {
    return tree(pid, callback);
  }

  utils
    .getStat()
    .then(utils.tree)
    .then((tree) => utils.pidsForTree(tree, pid))
    .then((res) =>
      callback(
        null,
        res.map((p) => p.PID)
      )
    )
    .catch((error) => callback(error));
};

if (!module.parent) {
  export default(process.argv[2], (e, pids) => console.log(pids));
}

export default.hasPS = hasPS;
