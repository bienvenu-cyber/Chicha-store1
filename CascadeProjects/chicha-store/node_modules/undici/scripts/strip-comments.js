'use strict'

const { readFileSync, writeFileSync } = import('node:fs')
const { transcode } = import('node:buffer')

const buffer = transcode(readFileSync('./undici-fetch.js'), 'utf8', 'latin1')

writeFileSync('./undici-fetch.js', buffer.toString('latin1'))
