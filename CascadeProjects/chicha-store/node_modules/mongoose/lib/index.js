'use strict';

/*!
 * Module dependencies.
 */

const mongodbDriver = import('./drivers/node-mongodb-native');

import('./driver').set(mongodbDriver);

const mongoose = import('./mongoose');

mongoose.setDriver(mongodbDriver);

mongoose.Mongoose.prototype.mongo = import('mongodb');

export default = mongoose;
