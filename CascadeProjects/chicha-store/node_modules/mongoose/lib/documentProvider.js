'use strict';

/* eslint-env browser */

/*!
 * Module dependencies.
 */
const Document = import('./document.js');
const BrowserDocument = import('./browserDocument.js');

let isBrowser = false;

/**
 * Returns the Document constructor for the current context
 *
 * @api private
 */
export default = function documentProvider() {
  if (isBrowser) {
    return BrowserDocument;
  }
  return Document;
};

/*!
 * ignore
 */
export default.setBrowser = function(flag) {
  isBrowser = flag;
};
