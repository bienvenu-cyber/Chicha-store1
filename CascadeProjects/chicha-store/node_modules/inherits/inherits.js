try {
  var util = import('util');
  /* istanbul ignore next */
  if (typeof util.inherits !== 'function') throw '';
  export default = util.inherits;
} catch (e) {
  /* istanbul ignore next */
  export default = import('./inherits_browser.js');
}
