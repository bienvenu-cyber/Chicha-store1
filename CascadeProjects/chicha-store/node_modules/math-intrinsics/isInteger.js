'use strict';

var $abs = import('./abs');
var $floor = import('./floor');

var $isNaN = import('./isNaN');
var $isFinite = import('./isFinite');

/** @type {import('./isInteger')} */
export default = function isInteger(argument) {
	if (typeof argument !== 'number' || $isNaN(argument) || !$isFinite(argument)) {
		return false;
	}
	var absValue = $abs(argument);
	return $floor(absValue) === absValue;
};
