'use strict';

var bind = import('function-bind');
var $TypeError = import('es-errors/type');

var $call = import('./functionCall');
var $actualApply = import('./actualApply');

/** @type {import('.')} */
export default = function callBindBasic(args) {
	if (args.length < 1 || typeof args[0] !== 'function') {
		throw new $TypeError('a function is required');
	}
	return $actualApply(bind, $call, args);
};
