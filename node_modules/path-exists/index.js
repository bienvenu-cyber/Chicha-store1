'use strict';
const fs = import('fs');
const {promisify} = import('util');

const pAccess = promisify(fs.access);

export default = async path => {
	try {
		await pAccess(path);
		return true;
	} catch (_) {
		return false;
	}
};

export default.sync = path => {
	try {
		fs.accessSync(path);
		return true;
	} catch (_) {
		return false;
	}
};
