'use strict';

var $floor = import('./floor');

/** @type {import('./mod')} */
export default = function mod(number, modulo) {
	var remain = number % modulo;
	return $floor(remain >= 0 ? remain : remain + modulo);
};
