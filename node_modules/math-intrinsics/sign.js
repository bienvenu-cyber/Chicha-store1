'use strict';

var $isNaN = import('./isNaN');

/** @type {import('./sign')} */
export default = function sign(number) {
	if ($isNaN(number) || number === 0) {
		return number;
	}
	return number < 0 ? -1 : +1;
};
