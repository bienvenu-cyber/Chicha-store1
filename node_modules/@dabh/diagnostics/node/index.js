//
// Select the correct build version depending on the environment.
//
if (process.env.NODE_ENV === 'production') {
  export default = import('./production.js');
} else {
  export default = import('./development.js');
}
