var adapter = import('./');

/**
 * Extracts the values from process.env.
 *
 * @type {Function}
 * @public
 */
export default = adapter(function storage() {
  return localStorage.getItem('debug') || localStorage.getItem('diagnostics');
});
