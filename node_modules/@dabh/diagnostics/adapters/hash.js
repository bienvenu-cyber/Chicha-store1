var adapter = import('./');

/**
 * Extracts the values from process.env.
 *
 * @type {Function}
 * @public
 */
export default = adapter(function hash() {
  return /(debug|diagnostics)=([^&]+)/i.exec(window.location.hash)[2];
});
