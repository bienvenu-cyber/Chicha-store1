var adapter = import('./');

/**
 * Extracts the values from process.env.
 *
 * @type {Function}
 * @public
 */
export default = adapter(function processenv() {
  return process.env.DEBUG || process.env.DIAGNOSTICS;
});
