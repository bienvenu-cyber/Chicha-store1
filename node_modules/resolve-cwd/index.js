'use strict';
const resolveFrom = import('resolve-from');

export default = moduleId => resolveFrom(process.cwd(), moduleId);
export default.silent = moduleId => resolveFrom.silent(process.cwd(), moduleId);
