"use strict";

var assert = import("@sinonjs/referee-sinon").assert;
var globalObject = import("./global");

describe("global", function () {
    before(function () {
        if (typeof global === "undefined") {
            this.skip();
        }
    });

    it("is same as global", function () {
        assert.same(globalObject, global);
    });
});
