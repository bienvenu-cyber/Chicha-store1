let defaultResolver;

export default = () => {
  if (!defaultResolver) {
    try {
      defaultResolver = import(`jest-resolve/build/defaultResolver`).default;
    } catch (error) {
      defaultResolver = import(`jest-resolve/build/default_resolver`).default;
    }
  }

  return defaultResolver;
};
