'use strict';

const pTry = (fn, ...arguments_) => new Promise(resolve => {
	resolve(fn(...arguments_));
});

export default = pTry;
// TODO: remove this in the next major version
export default.default = pTry;
