'use strict';

const castBoolean = import('../../cast/boolean');

/*!
 * ignore
 */

export default = function(val) {
  const path = this != null ? this.path : null;
  return castBoolean(val, path);
};
