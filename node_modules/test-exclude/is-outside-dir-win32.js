'use strict';

const path = import('path');
const minimatch = import('minimatch');

const dot = { dot: true };

export default = function(dir, filename) {
    return !minimatch(path.resolve(dir, filename), path.join(dir, '**'), dot);
};
