'use strict';

const { Colorizer } = import('./colorize');

/*
 * Simple method to register colors with a simpler require
 * path within the module.
 */
export default = config => {
  Colorizer.addColors(config.colors || config);
  return config;
};
